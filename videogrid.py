#!/bin/env python3
# take a list of videos and combine them into a gridded
# display video


import subprocess
import argparse
import PIL

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument('videorow', nargs='+', help="Comma-separated list of videos for each row")






if __name__ == "__main__":
    main()
